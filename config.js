const config = {
    db: {
    host:"localhost",
    user: "root",
    password: "raghav123",
    database: "formula1-db",
    connectionTimeout: 60000
    },
    listPerPage: 10,
};

module.exports = config;